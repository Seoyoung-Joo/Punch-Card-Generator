/**
 * TensionControl — slider 1–5.
 * On change: calls cardStore.setTension(n), which rebuilds the card with the new hash key.
 * Same word at different tension = different hole pattern.
 */
export default function TensionControl() {
  // TODO
  return null
}
