/**
 * RowDragLayer — drag-and-drop for blank ε rows using @dnd-kit/sortable.
 * Allows user to reorder rows within the card.
 * On drop: calls cardStore.insertEpsilonAt / removeEpsilonAt as needed.
 *
 * DraggableRow — sortable wrapper around a single card row.
 * Exported for use inside CardGrid.
 */

/** @param {{ rowIndex: number, row: boolean[] }} props */
export function DraggableRow({ rowIndex, row }) {
  // TODO
  return null
}

/** Custom drag overlay shown while dragging */
export default function RowDragLayer() {
  // TODO
  return null
}
